// STUB - av-ui worker owns this file (NOT the world worker).
// Mount point (inside Canvas) for the discoverable delights: collectible sun
// shards along and off the road, pickup detection (distance to telemetry
// .carPosition), air-time tracking. Placement derives from core/terrain's
// roadSpline so it survives circuit changes.
export function Delights() {
  return null
}
