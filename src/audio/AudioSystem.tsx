// STUB - av-ui worker replaces this file.
// Contract: procedurally-synthesised engine audio (WebAudio, no samples) whose
// pitch/volume track telemetry.rpm and throttle; impact thump from
// telemetry.impact; drift/skid layer from telemetry.slip. Starts on first user
// gesture (browser autoplay policy). No external audio files.
export function AudioSystem() {
  return null
}
